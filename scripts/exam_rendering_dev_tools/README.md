# These files are for improving exam generation

Before editing the LaTeX builder in `latex_exam_pdf_rendering.py`, first experiment with the templates in this folder until the layout looks correct, then move the final changes into the Python file.

For small layout tweaks, it is much faster to compile LaTeX directly here with the VS Code extension than to run the full generation script for every change.
